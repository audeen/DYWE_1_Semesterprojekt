{% extends "_layout" %}

{% block aktuelles %}
    
{% for entry in craft.entries.section('aktuelles').limit(1).all() %}

<div class="artikel_1">
    <a href='aktuellesOverview'>
    <h3>{{ entry.section }}</h3>
    </a>
    <a href="{{ entry.url }}">
    <h1>{{ entry.title }}</h1>
    </a>
    <h3>{{ entry.artikelTeaser }}</h3>
    {{ entry.artikelBody }}
</div>

{% endfor %}

{% endblock aktuelles %}


{% block projekte %}
    
{% for entry in craft.entries.section('projekte').limit(1).all() %}

<div class="artikel_1">
    <a href='projekteOverview'>
        <h3>{{ entry.section }}</h3>
    </a>
    <a href="{{ entry.url }}">
    <h1>{{ entry.title }}</h1>
    </a>
    <h3>{{ entry.artikelTeaser }}</h3>
    {{ entry.artikelBody }}
</div>

{% endfor %}

{% endblock projekte %}

{% block calltoaction %}

                <div class="cta0">
                    <div class="cta1">
                        <a href="{{ siteUrl }}projekteOverview">
                        <img class="cta_img" src="{{ siteUrl }}assets/imgs/pearl-syncussion-sy1-1529186.jpg">
                        <div class="cta4">Projekte</div>
                        </a>
                    </div>
                    <div class="cta2">
                        <a href="{{ siteUrl }}opensourceOverview">
                        <img class="cta_img" src="{{ siteUrl }}assets/imgs/desktop-source-code-and-wallpaper-by-computer-language-with-coding-and-programming_33771-596.jpg">
                        <div class="cta4">Open Source</div>
                        </a>
                    </div>
                    <div class="cta3">
                        <a href="{{ siteUrl }}modsOverview">
                        <img class="cta_img" src="{{ siteUrl }}assets/imgs/soldering.jpg">
                        <div class="cta4">Mods</div>
                        </a>
                    </div>
                </div>

{% endblock calltoaction %}
  
{% block opensource %}
    
{% for entry in craft.entries.section('opensource').limit(1).all() %}

<div class="artikel_1">
    <a href='opensourceOverview'>
        <h2>{{ entry.section }}</h2>
    </a>
    <a href="{{ entry.url }}">
    <h1>{{ entry.title }}</h1>
    </a>
    <h3>{{ entry.artikelTeaser }}</h3>
    {{ entry.artikelBody }}
</div>

{% endfor %}

{% endblock opensource %}


{% block mods %}

{% for entry in craft.entries.section('mods').limit(1).all() %}

<div class="artikel_1">
    <a href='modsOverview'>
        <h2>{{ entry.section }}</h2>
    </a>
    <a href="{{ entry.url }}">
    <h1>{{ entry.title }}</h1>
    </a>
    <h3>{{ entry.artikelTeaser }}</h3>
    {{ entry.artikelBody }}
</div>

{% endfor %}
<br>
{% endblock mods %}
