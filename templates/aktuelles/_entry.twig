{% extends "_layout" %}

{% set aktuelles = 'current' %}
{% set start, projekte, opensource, mods = '','','','' %}

{% block aktuelles %}


{#     <a href="{{ entry.url }}">
        <h1>{{ entry.title }}</h1>
    </a>
    <h3>{{ entry.artikelTeaser }}</h3>
    <h4>
        {% for image in entry.artikelImage.limit(4).all() %}
            <div>
                <img src="{{ image.geturl ('largeThumb') }}">
            </div>
        {% endfor %}
        {{ entry.artikelBody }}
    </h4> #}



  <div class="artikel_3">
    {% for block in entry.artikel.all() %}
	{% switch block.type %}

		{% case "artikelTitel" %}
			<h1>{{ block.titel }}</h1> {# Type: Reiner Text #}

		{% case "artikelTeaser" %}
			<h2>{{ block.teaser }}</h2> {# Type: Reiner Text #}

		{% case "artikelBody" %}
			{{ block.body }} {# Type: CKEditor #}

		{% case "bilder" %}
			<img src="{{ block.bildLarge.one.url }}"> {# Type: Dateien #}
			{# {{ block.bildExtern }} #} {# Type: URL #}
			{# {{ block.artikelThumbnail }} #} {# Type: Dateien #}

		{% case "videos" %}
			{{ block.youtubeEmbed }} {# Type: URL #}

		{% default %}
			...

	{% endswitch %}
{% endfor %}
    </div>



{% endblock aktuelles %}


