{% extends "_layout" %}

{% set start, aktuelles, projekte, opensource, mods = '','','','','' %}

{% block links %}

<div>
<div class="artikel_2">
    <h1>{{ entry.title }}</h1>
    <h3>{{ entry.artikelTeaser }}</h3>
    <h4>{{ entry.artikelBody }}</h4>
</div>
</div>

{% endblock links %}
